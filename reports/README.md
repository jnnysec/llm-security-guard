# Red-team Reports

Runtime reports generated by `/redteam/run?save=true` are written here as JSON and CSV.

The generated report files are intentionally ignored by git because they may contain model outputs or sensitive test artifacts.
